spring:
  application.name: sample-service
#  optional
  profiles.active: dev #dev/prod or default
#  default
  cloud.config.uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: "*"